# ============================================================
# Versoes fixadas para garantir compatibilidade.
#
# Conflito conhecido: torch < 2.5 + transformers >= 4.43 causa
# AttributeError em torch.fx.experimental.sym_node.DynamicInt.
# Solucao: torch >= 2.5 (removeu a dependencia de DynamicInt)
#          com transformers >= 4.46 (compativel com torch 2.5+).
# ============================================================

# PyTorch — versao minima que nao usa DynamicInt
torch>=2.5.0

# Transformers — compativel com torch 2.5+
transformers>=4.46.0,<5.0.0

# NLP & Embeddings
sentence-transformers>=3.3.0,<4.0.0

# LangChain ecosystem
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0,<0.4.0
langchain-openai>=0.2.0          # opcional: LLM_PROVIDER=openai

# Vector Store
faiss-cpu>=1.8.0

# Document Loaders
pypdf>=4.0.0                     # PDF
unstructured>=0.14.0             # docx, html, etc. (opcional)

# LLM local (Ollama)
# instale Ollama em: https://ollama.com/download
# depois: ollama pull llama3.2

# Interface
streamlit>=1.38.0
watchdog>=4.0.0

# Data & Utils
pandas>=2.0.0
numpy>=1.26.0
python-dotenv>=1.0.0
pydantic>=2.0.0
